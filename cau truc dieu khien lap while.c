#include<stdio.h>
int main(int argc, char** argv)
{
    /* while(dieu kien lap)
    {//noi dung can lap}
    trong do:
    -dieu kien lap la mot gia tri hoac bieu thuc co tinh dung
    hoac sai.
    -noi dung can lap de trong phan than vong lap co the gom 1
    hoac nhieu cau lenh.*/
    int count = 0,x;
    while( count <= 12)
    {
        printf("Gia tri count= %d\n",count);
      ++count;
    }
}
